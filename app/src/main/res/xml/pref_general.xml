<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="save_username"
        android:summary="@string/pref_description_save_username"
        android:title="@string/pref_title_save_username" />

    <ListPreference
        android:title="@string/pref_image_compression_level"
        android:key="image_compression"
        android:entries="@array/pref_image_compression_text"
        android:entryValues="@array/pref_image_compression_values" />

</PreferenceScreen>
